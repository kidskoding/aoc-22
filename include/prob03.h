#ifndef PROB03_H
#define PROB03_H

#include <set>

int prioritize();
int prioritizeInGroups();
char commonChar(std::set<char> one, std::set<char> two, std::set<char> three);
int helper(char c);

#endif // PROB03_H